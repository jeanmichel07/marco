{% extends 'base.html.twig' %}

{% block title %}Agence de voyage{% endblock %}

{% block body %}
    <h1>LocVoiture</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ loc_voiture.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ loc_voiture.type }}</td>
            </tr>
            <tr>
                <th>Capacite</th>
                <td>{{ loc_voiture.capacite }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ loc_voiture.prix }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_loc_voiture_index') }}">back to list</a>

    <a href="{{ path('app_loc_voiture_edit', {'id': loc_voiture.id}) }}">edit</a>

    {{ include('loc_voiture/_delete_form.html.twig') }}
{% endblock %}
