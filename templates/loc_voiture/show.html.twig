{% extends 'base.html.twig' %}

{% block title %}Agence de voyage{% endblock %}

{% block body %}
    <h1 style="text-align: center">Voiture</h1>

    <table class="table" style="text-align: center">
        <tbody>
            <tr >
                <th class="bg-success">N°</th>
                <td>{{ loc_voiture.id }}</td>
            </tr>
            <tr>
                <th class="bg-success">Type</th>
                <td>{{ loc_voiture.type }}</td>
            </tr>
            <tr>
                <th class="bg-success">Capacite</th>
                <td>{{ loc_voiture.capacite }}</td>
            </tr>
            <tr>
                <th class="bg-success">Prix</th>
                <td>{{ loc_voiture.prix }}</td>
            </tr>
        </tbody>
    </table>
<div style="text-align: center; display: flex; padding-bottom: 120px">
    <a class="btn btn-secondary text-white p-1 "style="text-decoration: none;" href="{{ path('app_loc_voiture_index') }}">Retour</a>&nbsp;

    <a class="btn btn-warning text-white p-1 "style="text-decoration: none;" href="{{ path('app_loc_voiture_edit', {'id': loc_voiture.id}) }}">Modifier</a>&nbsp;

    {{ include('loc_voiture/_delete_form.html.twig') }}
</div>
{% endblock %}
