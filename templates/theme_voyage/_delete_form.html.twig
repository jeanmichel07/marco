<form method="post" action="{{ path('app_theme_voyage_delete', {'id': theme_voyage.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ theme_voyage.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
