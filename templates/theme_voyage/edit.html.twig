{% extends 'base.html.twig' %}

{% block title %}Agence de voyage{% endblock %}

{% block body %}
    <h1 style="text-align: center">Modifier&nbsp;{{ theme_voyage.name }}:&nbsp;{{ theme_voyage.titre }}</h1>

    {{ include('theme_voyage/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-secondary" href="{{ path('app_theme_voyage_index') }}">Retour</a>

    {{ include('theme_voyage/_delete_form.html.twig') }}
{% endblock %}
