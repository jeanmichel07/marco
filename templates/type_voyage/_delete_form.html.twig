<form method="post" action="{{ path('app_type_voyage_delete', {'id': type_voyage.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_voyage.id) }}">
    <button class="btn btn-danger text-white p-1 "style="text-decoration: none;">Supprimer</button>
</form>
